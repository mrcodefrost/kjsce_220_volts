import 'package:flutter/material.dart';

class GuideLines extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.white,
      body: Column(
        children: <Widget>[
          Center(
            child: Image.asset('assets/construction_page.png'),
          )
        ],
      ),
    );
  }
}
